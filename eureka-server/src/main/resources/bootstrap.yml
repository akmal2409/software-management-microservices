spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8071
    inetutils:
      ignored-interfaces:
        - docker
        - eth.*
      preferred-networks:
        - 10.0.0
server:
  port: 8070


